<div class="page-width">
    <div class="grid">
      <div class="content-container">
        <div class="section-header text-center">
          <h1>{{ page.title }}</h1>
        </div>

        {% section 'text-and-image' %}
        {% section 'chained-text' %}
  
        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
